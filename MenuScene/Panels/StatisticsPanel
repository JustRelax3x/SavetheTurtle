using UnityEngine;
using UnityEngine.UI;

public class StatisticsPanel : ViewPanel {
    [SerializeField]
    private Button _back;

    private void Start() {
        _back.onClick.AddListener(() => Close(PanelType.MainMenu));
    }

    private void OnDestroy {
        _back.onClick.RemoveAllListeners();
    }
}